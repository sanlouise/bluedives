<%= link_to "Create your profile!", new_user_profile_path(current_user) if current_user %>

<div class="jumbotron text-center">
    <h1>Mentor Hub</h1>
    <h3>Where go-getters meet lifelong mentors.</h3>
</div>

<div class="row text-center">
    <div class="col-md-6"> 
        <div class="well">
            <h2>Basic Membership</h2>
            <h4>A free account gives you access to our community!</h4>
            <%= link_to "Free Sign Up", new_user_registration_path(plan: @basic_plan.id), class: 'btn btn-success btn-lg' %>
        </div>
    </div>
    
    <div class="col-md-6"> 
        <div class="well">
            <h2>Premium Membership</h2>
            <h4>A premium account gives you access to our secret areas.</h4>
            <%= link_to "Sign Up", new_user_registration_path(plan: @pro_plan.id), class: 'btn btn-success btn-lg' %>
        </div>
    </div>
</div>